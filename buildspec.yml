version: 0.2
phases:
  pre_build:
    commands:
    - echo "Entered the pre-build phase"
  build:
    commands:
    - echo "Entered build phase"
  post_build:
    commands:
    - echo "Entered post build phase"
    - mvn package
    - mv target/bluegreendeploymentdemo-v1.war bluegreendeploymentdemo-v1.war
artifacts:
  files:
    - bluegreendeploymentdemo-v1.war